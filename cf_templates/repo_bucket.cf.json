{
    "Resources" : {
        "RepoBucket" : {
            "Type" : "AWS::S3::Bucket",
            "Properties" : {
                "AccessControl" : "Private",
                "BucketName" : { "Fn::Sub" : "tenzing-${AWS::AccountId}-${AWS::Region}" }
            }
        }
    }
}
